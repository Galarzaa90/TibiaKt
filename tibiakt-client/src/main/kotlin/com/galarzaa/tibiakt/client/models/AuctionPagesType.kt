package com.galarzaa.tibiakt.client.models

internal enum class AuctionPagesType(val typeId: Int) {
    ITEMS(0),
    ITEMS_STORE(1),
    MOUNTS(2),
    MOUNTS_STORE(3),
    OUTFITS(4),
    OUTFITS_STORE(5)
}